형변환과 증감 연산자는 낮설듯.
c언어에서 / 는 정수 / 정수 = 정수 임 10/3 = 3
자료형이 다르면 산술 연산이 안된다. e.g 1+2.3 = error.
대입연산자가 우선순위 가장 낮고, 단항이 이항보다 우선순위 높음.

avg = (double) sum / n; () -> / -> =

++n <-> n++ 모든 연산자는 연산의 행위와 더불어 연산의 결과값을 가지는데, 여기서는 결과값이 다르다. 하나는 결과값이 0이고 또 다른건 1이된다.
증가하고 결과값을 가진다. 결과값을 가지고 증가한다. n+m 은 결과값을 버린다. n++는 피연산자가 바뀌는데, 결과값이
n++ 는 결과값을 가지고 증가한다. n 은 0

sizeof - 함수 아니고 연산자임